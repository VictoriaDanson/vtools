{
  "name": "vtools-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root project for vtools with server backend",
  "scripts": {
    "dev:server": "pnpm --filter server dev",
    "dev:moment-flow": "pnpm --filter moment-flow dev",
    "start:server": "pnpm --filter server start",
    "build:moment-flow": "pnpm --filter moment-flow build"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
